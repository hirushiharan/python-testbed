[project]
name = "python-testbed"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "pypandoc-binary>=1.13",
    "python-dotenv>=1.2.1",
]
